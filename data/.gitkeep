# This file exists only to make Git track the empty data/ folder.
# The server automatically creates users.dat and chat.key here at runtime.
# Both those files are excluded by .gitignore (they contain secrets/user data).
